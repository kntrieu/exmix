import { createMuiTheme } from '@material-ui/core/styles';

const exmixThemes = createMuiTheme({
    palette: {
        primary: {
            main: '#11BFAE',
            contrastText: 'white'
        }
    },
});

export default exmixThemes;